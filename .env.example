# Який провайдер даних використовувати: mock | cliniccards (дефолт: mock)
PROVIDER=mock

# Cliniccards CRM credentials (server-only — never expose to the client)
# Реальні значення тримай у локальному .env (він у .gitignore), не тут.
CLINICCARDS_API_KEY=
CLINICCARDS_BASE_URL=
